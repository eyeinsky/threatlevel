module Web.CSS where

import X.Prelude
-- import Web.Monad
import CSS

-- reset :: MonadWeb m => m ()
-- reset = do
--   cssRule (Tag "html") (boxSizing "border-box")
--   cssRule (Tag "ul") (listStyle "none")
--   cssRule CSS.Any $ do
--     boxSizing "inherit"
--     zero
--   where
--     zero = do
--       padding $ px 0
--       margin $ px 0
