module Web.SVG
  ( module Web.SVG.Elements
  , module Web.SVG.Attributes
  ) where

import Web.SVG.Elements hiding (clipPath, colorProfile, cursor, filter, glyphRef, mask, style)
import Web.SVG.Attributes hiding (clipPath, colorProfile, cursor, filter, glyphRef, mask, style)
